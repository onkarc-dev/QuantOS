# PRISMFlow API requirements
# ─── Core (required) ────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
pydantic-settings==2.4.0
email-validator==2.2.0
python-multipart==0.0.9

# ─── Auth / security ─────────────────────────────────────────────────────────
PyJWT==2.8.0

# ─── Database (SQLite is stdlib; psycopg2 only needed for PostgreSQL) ─────────
# psycopg2-binary==2.9.9  # Uncomment for PostgreSQL production

# ─── Job queue (Redis/RQ only needed if REDIS_URL is set) ────────────────────
# rq==1.16.2              # Uncomment for Redis Queue production
# redis==5.0.1            # Uncomment for Redis production

# ─── Configuration ────────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ─── Dev / test only ──────────────────────────────────────────────────────────
httpx==0.27.0            # FastAPI test client and smoke scripts
# pytest==8.3.0           # Uncomment for pytest (stdlib unittest works too)
# pytest-asyncio==0.23.8  # Uncomment for async tests
